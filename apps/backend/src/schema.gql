# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddTimeEntryInput {
  endedAt: DateTime!
  notes: String
  sessionId: String!
  startedAt: DateTime!
}

input ApproveVolunteerSessionInput {
  id: String!
  organizationId: String!
}

input CreateOrganizationInput {
  address: String
  description: String
  email: String
  logoUrl: String
  name: String!
  parentId: String
  phone: String
  websiteUrl: String
}

input CreateProjectInput {
  description: String!
  endsAt: DateTime!
  location: String!
  organizationId: ID!
  startsAt: DateTime!
  status: ProjectStatus! = DRAFT
  title: String!
}

input CreateShiftInput {
  endsAt: DateTime!
  instructions: String
  invitedMemberIds: [String!]
  location: String
  projectId: String
  startsAt: DateTime!
  title: String!
  visibility: ShiftVisibility! = ALL_MEMBERS
}

input CreateTaskAssignmentInput {
  assigneeId: String!
  organizationId: String!
  taskId: String!
}

input CreateTaskInput {
  description: String!
  dueDate: DateTime!
  organizationId: String!
  projectId: String!
  status: TaskStatus! = TODO
  title: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Membership {
  id: ID!
  organization: Organization!
  role: OrganizationRole!
  user: User!
}

type MembershipRequest {
  createdAt: DateTime!
  id: ID!
  organization: Organization!
  rejectionReason: String!
  reviewedAt: DateTime!
  reviewedBy: User!
  status: MembershipRequestStatus!
  updatedAt: DateTime!
  user: String!
}

enum MembershipRequestStatus {
  ACCEPTED
  CANCELLED
  PENDING
  REJECTED
}

type Mutation {
  addTimeEntry(input: AddTimeEntryInput!): TimeEntry!
  approveMembershipRequest(id: ID!, organizationId: ID!): MembershipRequest!
  approveVolunteerSession(input: ApproveVolunteerSessionInput!): VolunteerSession!
  assignTask(input: CreateTaskAssignmentInput!): Task!
  cancelMembershipRequest(id: ID!, organizationId: ID!): MembershipRequest!
  createMembershipRequest(organizationId: ID!): MembershipRequest!
  createOrganization(input: CreateOrganizationInput!): Organization!
  createProject(input: CreateProjectInput!): Project!
  createShift(input: CreateShiftInput!): Shift!
  createTask(input: CreateTaskInput!): Task!
  deleteShift(id: String!): Shift!
  deleteTimeEntry(id: ID!): TimeEntry!
  endVolunteerSession(id: ID!): VolunteerSession!
  inviteMembersToShift(memberIds: [String!]!, shiftId: String!): Shift!
  publishProject(id: ID!): Project!
  rejectMembershipRequest(id: ID!, organizationId: ID!, rejectionReason: String!): MembershipRequest!
  rejectVolunteerSession(input: RejectVolunteerSessionInput!): VolunteerSession!
  startVolunteerSession(input: StartVolunteerSessionInput!): VolunteerSession!
  updateProject(id: ID!, input: UpdateProjectInput!): Project!
  updateShift(id: String!, input: UpdateShiftInput!): Shift!
}

type Organization {
  address: String
  admins: [User!]
  children: [Organization!]!
  createdAt: DateTime!
  description: String
  email: String
  id: ID!
  logoUrl: String
  moderators: [User!]
  name: String!
  parent: Organization
  phone: String
  projects(limit: Int! = 10, offset: Int! = 0): [Project!]
  slug: String!
  updatedAt: DateTime
  volunteers: [User!]
  websiteUrl: String
}

type OrganizationPaginatedResponse {
  items: [Organization!]!
  pagination: PaginationInfo!
}

enum OrganizationRole {
  ADMIN
  VOLUNTEER
}

type PaginationInfo {
  hasMore: Boolean!
  limit: Int!
  offset: Int!
  total: Int!
}

type Project {
  createdAt: DateTime!
  createdBy: User!
  description: String!
  endsAt: DateTime!
  id: ID!
  location: String!
  organization: Organization!
  slug: String!
  startsAt: DateTime!
  status: ProjectStatus!
  tasks: [Task!]
  title: String!
  updatedAt: DateTime
}

type ProjectPaginatedResponse {
  items: [Project!]!
  pagination: PaginationInfo!
}

enum ProjectStatus {
  ACTIVE
  ARCHIVED
  DRAFT
  EXPIRED
}

type Query {
  isUserOrganizationAdmin(organizationId: ID!): Boolean!
  isUserOrganizationMember(organizationId: ID!): Boolean!
  isUserOrganizationStaff(organizationId: ID!): Boolean!
  isUserOrganizationVolunteer(organizationId: ID!): Boolean!
  me: User!
  membership(organizationId: ID!): Membership!
  membershipRequests(limit: Int! = 10, offset: Int! = 0, organizationId: ID!): MembershipRequest!
  myTasks(limit: Int! = 10, offset: Int! = 0): TaskPaginatedResponse!
  organization(id: String!): Organization!
  organizationBySlug(slug: String!): Organization!
  organizations(limit: Int! = 10, offset: Int! = 0): OrganizationPaginatedResponse!
  project(id: ID!): Project!
  projects(limit: Int! = 10, offset: Int! = 0, organizationId: ID!): ProjectPaginatedResponse!
  shift(id: String!): Shift!
  shifts(limit: Int! = 10, offset: Int! = 0): ShiftPaginatedResponse!
  shiftsByProjectId(limit: Int! = 10, offset: Int! = 0, projectId: String!): ShiftPaginatedResponse!
  task(id: String!): Task!
  tasksByUserId(limit: Int! = 10, offset: Int! = 0, userId: String!): TaskPaginatedResponse!
  user(id: String!): User
  usersByRole(organizationId: ID!, role: OrganizationRole!): User!
  volunteerSessions(status: VolunteerSessionStatus): [VolunteerSession!]!
}

input RejectVolunteerSessionInput {
  id: String!
  organizationId: String!
  rejectionReason: String
}

type Shift {
  createdBy: User!
  endsAt: DateTime!
  id: ID!
  instructions: String
  location: String
  organization: Organization!
  project: Project
  slug: String!
  startsAt: DateTime!
  title: String!
  visibility: ShiftVisibility!
  volunteers: [User!]
}

type ShiftPaginatedResponse {
  items: [Shift!]!
  pagination: PaginationInfo!
}

enum ShiftVisibility {
  ALL_MEMBERS
  INVITED_MEMBERS
}

input StartVolunteerSessionInput {
  shiftId: String!
  status: VolunteerSessionStatus! = SUBMITTED
  volunteerId: String!
}

type Task {
  assignees: [User!]
  createdAt: DateTime!
  createdBy: User!
  description: String!
  dueDate: DateTime!
  id: ID!
  project: Project!
  slug: String!
  status: TaskStatus!
  title: String!
  updatedAt: DateTime
}

type TaskPaginatedResponse {
  items: [Task!]!
  pagination: PaginationInfo!
}

enum TaskStatus {
  ARCHIVED
  DONE
  IN_PROGRESS
  TODO
}

type TimeEntry {
  createdAt: DateTime!
  endedAt: DateTime!
  id: ID!
  notes: String
  startedAt: DateTime!
}

input UpdateProjectInput {
  description: String
  endsAt: DateTime
  location: String
  organizationId: ID
  startsAt: DateTime
  status: ProjectStatus = DRAFT
  title: String
}

input UpdateShiftInput {
  endsAt: DateTime
  instructions: String
  invitedMemberIds: [String!]
  location: String
  projectId: String
  startsAt: DateTime
  title: String
  visibility: ShiftVisibility = ALL_MEMBERS
}

type User {
  email: String!
  id: ID!
  image: String
  name: String!
}

type VolunteerSession {
  createdAt: DateTime!
  entries: [TimeEntry!]!
  id: ID!
  rejectionReason: String
  shift: Shift
  status: VolunteerSessionStatus!
  task: Task!
  updatedAt: DateTime
  user: User!
  validatedAt: DateTime
  validatedBy: User
}

enum VolunteerSessionStatus {
  APPROVED
  CANCELLED
  IN_PROGRESS
  PENDING
  REJECTED
  SUBMITTED
}