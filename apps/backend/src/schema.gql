# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddTimeRecordInput {
  endedAt: DateTime!
  notes: String
  sessionId: String!
  startedAt: DateTime!
}

input ApproveTimeSessionInput {
  id: String!
  organizationId: String!
}

input CreateOrganizationInput {
  address: String
  description: String
  email: String
  logoUrl: String
  name: String!
  parentId: String
  phone: String
  websiteUrl: String
}

input CreateProjectInput {
  description: String!
  endsAt: DateTime!
  location: String!
  organizationId: String!
  startsAt: DateTime!
  status: ProjectStatus! = DRAFT
  title: String!
}

input CreateTaskAssignmentInput {
  assigneeId: String!
  organizationId: String!
  taskId: String!
}

input CreateTaskInput {
  description: String!
  dueDate: DateTime!
  organizationId: String!
  projectId: String!
  status: TaskStatus! = TODO
  title: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  addTimeRecord(input: AddTimeRecordInput!): TimeRecord!
  approveTimeSession(input: ApproveTimeSessionInput!): TimeSession!
  assignTask(input: CreateTaskAssignmentInput!): Task!
  createOrganization(input: CreateOrganizationInput!): Organization!
  createProject(input: CreateProjectInput!): Project!
  createTask(input: CreateTaskInput!): Task!
  deleteTimeRecord(id: ID!): TimeRecord!
  endTimeSession(id: ID!): TimeSession!
  publishProject(id: ID!): Project!
  rejectTimeSession(input: RejectTimeSessionInput!): TimeSession!
  startTimeSession(input: StartTimeSessionInput!): TimeSession!
}

type Organization {
  address: String
  admins: [User!]
  children: [Organization!]!
  createdAt: DateTime!
  description: String
  email: String
  id: ID!
  logoUrl: String
  moderators: [User!]
  name: String!
  owner: User!
  parent: Organization
  phone: String
  projects(limit: Int! = 10, offset: Int! = 0): [Project!]
  slug: String!
  updatedAt: DateTime
  volunteers: [User!]
  websiteUrl: String
}

type OrganizationPaginatedResponse {
  items: [Organization!]!
  pagination: PaginationInfo!
}

type PaginationInfo {
  hasMore: Boolean!
  limit: Int!
  offset: Int!
  total: Int!
}

type Project {
  createdAt: DateTime!
  createdBy: User!
  description: String!
  endsAt: DateTime!
  id: ID!
  location: String!
  organization: Organization!
  slug: String!
  startsAt: DateTime!
  status: ProjectStatus!
  tasks: [Task!]
  title: String!
  updatedAt: DateTime
}

type ProjectPaginatedResponse {
  items: [Project!]!
  pagination: PaginationInfo!
}

enum ProjectStatus {
  ACTIVE
  ARCHIVED
  DRAFT
  EXPIRED
}

type Query {
  me: User!
  myTasks(limit: Int! = 10, offset: Int! = 0): TaskPaginatedResponse!
  organization(id: String!): Organization!
  organizationBySlug(slug: String!): Organization!
  organizations(limit: Int! = 10, offset: Int! = 0): OrganizationPaginatedResponse!
  project(id: String!): Project!
  projects(limit: Int! = 10, offset: Int! = 0, organizationId: String!): ProjectPaginatedResponse!
  shift(id: String!): Shift!
  shifts(limit: Int! = 10, offset: Int! = 0): ShiftPaginatedResponse!
  task(id: String!): Task!
  tasksByUserId(limit: Int! = 10, offset: Int! = 0, userId: String!): TaskPaginatedResponse!
  user(id: String!): User
}

input RejectTimeSessionInput {
  id: String!
  organizationId: String!
  rejectionReason: String
}

type Shift {
  assignees: [User!]
  createdBy: User!
  description: String!
  endsAt: DateTime!
  id: ID!
  location: String
  organization: Organization!
  project: Project
  startsAt: DateTime!
}

type ShiftPaginatedResponse {
  items: [Shift!]!
  pagination: PaginationInfo!
}

input StartTimeSessionInput {
  status: TimeSessionStatus! = IN_PROGRESS
  taskId: String!
}

type Task {
  assignees: [User!]
  createdAt: DateTime!
  createdBy: User!
  description: String!
  dueDate: DateTime!
  id: ID!
  project: Project!
  slug: String!
  status: TaskStatus!
  title: String!
  updatedAt: DateTime
}

type TaskPaginatedResponse {
  items: [Task!]!
  pagination: PaginationInfo!
}

enum TaskStatus {
  ARCHIVED
  DONE
  IN_PROGRESS
  TODO
}

type TimeRecord {
  createdAt: DateTime!
  endedAt: DateTime!
  id: ID!
  notes: String
  startedAt: DateTime!
}

type TimeSession {
  createdAt: DateTime!
  id: ID!
  records: [TimeRecord!]!
  rejectionReason: String
  status: TimeSessionStatus!
  task: Task!
  updatedAt: DateTime
  user: User!
  validatedAt: DateTime
  validatedBy: User
}

enum TimeSessionStatus {
  APPROVED
  CANCELLED
  IN_PROGRESS
  PENDING
  REJECTED
  SUBMITTED
}

type User {
  email: String!
  id: ID!
  image: String!
  name: String!
}