# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddTimeEntryInput {
  endedAt: DateTime
  notes: String
  shiftId: String!
  startedAt: DateTime!
  volunteerId: String!
}

input CloseTimeEntryInput {
  endedAt: DateTime!
  notes: String
}

input CreateOrganizationInput {
  address: String
  description: String
  email: String
  logoUrl: String
  name: String!
  parentId: String
  phone: String
  websiteUrl: String
}

input CreateRoleInput {
  description: String
  name: String!
  permissionIds: [String!]!
}

input CreateShiftInput {
  endsAt: DateTime!
  instructions: String
  invitedMemberIds: [String!]
  location: String
  startsAt: DateTime!
  title: String!
  visibility: ShiftVisibility! = ALL_MEMBERS
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Membership {
  id: ID!
  organization: Organization!
  user: User!
}

type MembershipRequest {
  createdAt: DateTime!
  id: ID!
  organization: Organization!
  rejectionReason: String
  reviewedAt: DateTime
  reviewedBy: User
  status: MembershipRequestStatus!
  updatedAt: DateTime!
  user: User!
}

type MembershipRequestPaginatedResponse {
  items: [MembershipRequest!]!
  pagination: PaginationInfo!
}

enum MembershipRequestStatus {
  ACCEPTED
  CANCELLED
  PENDING
  REJECTED
}

type Mutation {
  addTimeEntry(input: AddTimeEntryInput!): TimeEntry!
  approveMembershipRequest(id: ID!, organizationId: ID!): MembershipRequest!
  assignRoleToMembership(membershipId: ID!, roleId: ID!): Boolean!
  cancelMembershipRequest(id: ID!, organizationId: ID!): MembershipRequest!
  closeTimeEntry(id: ID!, input: CloseTimeEntryInput!): TimeEntry!
  createMembershipRequest(organizationId: ID!): MembershipRequest!
  createOrganization(input: CreateOrganizationInput!): Organization!
  createRole(input: CreateRoleInput!): Role!
  createShift(input: CreateShiftInput!): Shift!
  deleteRole(id: ID!): Role!
  deleteShift(id: String!): Shift!
  deleteTimeEntry(id: ID!): TimeEntry!
  inviteMembersToShift(memberIds: [String!]!, shiftId: String!): Shift!
  rejectMembershipRequest(id: ID!, organizationId: ID!, rejectionReason: String!): MembershipRequest!
  updateRole(id: ID!, input: CreateRoleInput!): Role!
  updateShift(id: String!, input: UpdateShiftInput!): Shift!
}

type Organization {
  address: String
  admins: [User!]
  children: [Organization!]!
  createdAt: DateTime!
  description: String
  email: String
  id: ID!
  logoUrl: String
  moderators: [User!]
  name: String!
  parent: Organization
  phone: String
  slug: String!
  updatedAt: DateTime
  volunteers: [User!]
  websiteUrl: String
}

type OrganizationPaginatedResponse {
  items: [Organization!]!
  pagination: PaginationInfo!
}

type OrganizationPublicInfo {
  address: String
  description: String
  email: String
  id: ID!
  logoUrl: String
  name: String!
  phone: String
  slug: String!
  websiteUrl: String
}

type PaginationInfo {
  hasMore: Boolean!
  limit: Int!
  offset: Int!
  total: Int!
}

type Permission {
  description: String
  id: ID!
  key: String!
}

type Query {
  activeShifts(limit: Int! = 10, offset: Int! = 0): ShiftPaginatedResponse!
  me: User!
  membership(organizationId: ID!): Membership!
  membershipRequests(limit: Int! = 10, offset: Int! = 0, organizationId: ID!, status: MembershipRequestStatus): MembershipRequestPaginatedResponse!
  organization(id: String!): Organization!
  organizationBySlug(slug: String!): Organization!
  organizationPublicInfo(id: String!): Organization!
  organizations(limit: Int! = 10, offset: Int! = 0): OrganizationPaginatedResponse!
  permissions: [Permission!]!
  roles: [Role!]!
  shift(id: String!): Shift!
  shifts(limit: Int! = 10, offset: Int! = 0): ShiftPaginatedResponse!
  timeEntries(limit: Int! = 10, offset: Int! = 0): TimeEntryPaginatedResponse!
  timeEntriesByUser(limit: Int! = 10, offset: Int! = 0, userId: String!): TimeEntryPaginatedResponse!
  user(id: String!): User
  userByCheckInId(checkInId: String!): User
}

type Role {
  description: String
  id: ID!
  isInternal: Boolean!
  name: String!
  permissions: [Permission!]!
}

type Shift {
  createdBy: User!
  endsAt: DateTime!
  id: ID!
  instructions: String
  location: String
  organization: Organization!
  slug: String!
  startsAt: DateTime!
  title: String!
  visibility: ShiftVisibility!
  volunteers: [User!]
}

type ShiftPaginatedResponse {
  items: [Shift!]!
  pagination: PaginationInfo!
}

enum ShiftVisibility {
  ALL_MEMBERS
  INVITED_MEMBERS
}

type TimeEntry {
  createdAt: DateTime!
  endedAt: DateTime
  id: ID!
  notes: String
  shift: Shift!
  startedAt: DateTime!
  volunteer: User!
}

type TimeEntryPaginatedResponse {
  items: [TimeEntry!]!
  pagination: PaginationInfo!
}

input UpdateShiftInput {
  endsAt: DateTime
  instructions: String
  invitedMemberIds: [String!]
  location: String
  startsAt: DateTime
  title: String
  visibility: ShiftVisibility = ALL_MEMBERS
}

type User {
  checkInId: ID!
  email: String!
  id: ID!
  image: String
  name: String!
  permissions: [Permission!]
}