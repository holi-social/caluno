// Legacy preset form configs — replaced by block-based architecture.
// Blocks are now stored in data/blocks.json, forms reference blocks via blockRefs.
// See store-blocks.ts and store-configs.ts for the new data model.

// import type { FormConfig } from './types';
// export const PRESET_FORM_CONFIGS: FormConfig[] = [ ... ];
