#!/usr/bin/env bash
# Local pre-commit hook — secret scan, then format/lint-fix staged files with Biome.
ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
"$ROOT/.githooks/pre-commit-secrets.sh" || exit 1
exec "$ROOT/.githooks/pre-commit-format.sh"
