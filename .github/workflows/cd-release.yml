# See SETUP.md at the repo root for the one-time setup this workflow
# depends on (secrets, runners, environment protection rules).
name: CD - Release

on:
  push:
    branches: [release]

env:
  TF_ROOT: packages/infra
  TF_IN_AUTOMATION: 'true'
  TF_INPUT: 'false'

jobs:
  check-migrations:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive
          token: ${{ secrets.ORG_REPO_PAT }}
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1'
      - run: bun install --frozen-lockfile
      - name: Check migrations are up to date
        run: |
          set -o pipefail
          OUTPUT="$(bun run --cwd apps/backend db:generate < /dev/null 2>&1 | tee /dev/stderr)"
          if ! echo "$OUTPUT" | grep -q "No schema changes, nothing to migrate"; then
            echo "✖ Schema changes are not reflected in committed migrations."
            echo "  Run 'bun run --cwd apps/backend db:generate' locally and commit the result."
            exit 1
          fi

  build-release-backend:
    needs: [check-migrations]
    runs-on: self-hosted
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive
          token: ${{ secrets.ORG_REPO_PAT }}
      - name: Log into Scaleway registry
        run: echo "${{ secrets.SCW_SECRET_KEY }}" | docker login "${{ vars.SCW_REGISTRY_HOST }}/${{ vars.SCW_REGISTRY_NAMESPACE }}" -u nologin --password-stdin
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push backend image
        env:
          REGISTRY_NAMESPACE: ${{ vars.SCW_REGISTRY_HOST }}/${{ vars.SCW_REGISTRY_NAMESPACE }}
          IMAGE_TAG_SHA: ${{ github.sha }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        run: |
          docker buildx build --load \
            --build-arg SENTRY_RELEASE="${{ github.sha }}" \
            --build-arg SENTRY_ORG="${{ vars.SENTRY_ORG }}" \
            --build-arg SENTRY_PROJECT_BACKEND="${{ vars.SENTRY_PROJECT_BACKEND }}" \
            --secret id=sentry_auth_token,env=SENTRY_AUTH_TOKEN \
            -t "$REGISTRY_NAMESPACE/caluno-backend:$IMAGE_TAG_SHA" \
            -t "$REGISTRY_NAMESPACE/caluno-backend:release" \
            -f apps/backend/Dockerfile .
          docker push "$REGISTRY_NAMESPACE/caluno-backend:$IMAGE_TAG_SHA"
          docker push "$REGISTRY_NAMESPACE/caluno-backend:release"

  build-release-frontend:
    needs: [check-migrations]
    runs-on: self-hosted
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive
          token: ${{ secrets.ORG_REPO_PAT }}
      - name: Log into Scaleway registry
        run: echo "${{ secrets.SCW_SECRET_KEY }}" | docker login "${{ vars.SCW_REGISTRY_HOST }}/${{ vars.SCW_REGISTRY_NAMESPACE }}" -u nologin --password-stdin
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push frontend image
        env:
          REGISTRY_NAMESPACE: ${{ vars.SCW_REGISTRY_HOST }}/${{ vars.SCW_REGISTRY_NAMESPACE }}
          IMAGE_TAG_SHA: ${{ github.sha }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        run: |
          docker buildx build --load \
            --build-arg NEXT_PUBLIC_API_URL="https://release.api.caluno.org" \
            --build-arg NEXT_PUBLIC_WEB_URL="https://release.app.caluno.org" \
            --build-arg NEXT_PUBLIC_SENTRY_DSN="${{ vars.NEXT_PUBLIC_SENTRY_DSN }}" \
            --build-arg NEXT_PUBLIC_SENTRY_ENVIRONMENT="release" \
            --build-arg SENTRY_RELEASE="${{ github.sha }}" \
            --build-arg SENTRY_ORG="${{ vars.SENTRY_ORG }}" \
            --build-arg SENTRY_PROJECT_FRONTEND="${{ vars.SENTRY_PROJECT_FRONTEND }}" \
            --secret id=sentry_auth_token,env=SENTRY_AUTH_TOKEN \
            -t "$REGISTRY_NAMESPACE/caluno-frontend:$IMAGE_TAG_SHA" \
            -t "$REGISTRY_NAMESPACE/caluno-frontend:release" \
            -f apps/frontend/Dockerfile .
          docker push "$REGISTRY_NAMESPACE/caluno-frontend:$IMAGE_TAG_SHA"
          docker push "$REGISTRY_NAMESPACE/caluno-frontend:release"

  terraform-release-plan:
    needs: [build-release-backend, build-release-frontend]
    runs-on: self-hosted
    permissions:
      contents: read
      packages: read
    concurrency: terraform-release
    container:
      image: ghcr.io/holi-social/mirror/terraform:1.15
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    env:
      TF_VAR_image_tag: ${{ github.sha }}
      TF_VAR_active_environment: release
      SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
      SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
      SCW_DEFAULT_PROJECT_ID: ${{ vars.SCW_DEFAULT_PROJECT_ID }}
      SCW_DEFAULT_REGION: ${{ vars.SCW_DEFAULT_REGION }}
      SCW_DEFAULT_ZONE: ${{ vars.SCW_DEFAULT_ZONE }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
      AWS_DEFAULT_REGION: ${{ vars.AWS_DEFAULT_REGION }}
    outputs:
      has_changes: ${{ steps.plan.outputs.has_changes }}
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive
          token: ${{ secrets.ORG_REPO_PAT }}
      - id: plan
        working-directory: ${{ env.TF_ROOT }}
        run: |
          terraform init -backend-config="key=release/terraform.tfstate"
          terraform validate
          set +e
          terraform plan -detailed-exitcode -var-file="terraform.tfvars" -out=plan-release.tfplan
          code=$?
          set -e
          if [ "$code" -eq 0 ]; then echo "has_changes=0" >> "$GITHUB_OUTPUT"
          elif [ "$code" -eq 2 ]; then echo "has_changes=1" >> "$GITHUB_OUTPUT"
          else exit "$code"; fi
      - uses: actions/upload-artifact@v7
        with:
          name: plan-release
          path: packages/infra/plan-release.tfplan
          retention-days: 1

  terraform-release-apply:
    needs: [terraform-release-plan]
    runs-on: self-hosted
    permissions:
      contents: read
      packages: read
    concurrency: terraform-release
    environment: release
    container:
      image: ghcr.io/holi-social/mirror/terraform:1.15
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    env:
      TF_VAR_active_environment: release
      SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
      SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
      SCW_DEFAULT_PROJECT_ID: ${{ vars.SCW_DEFAULT_PROJECT_ID }}
      SCW_DEFAULT_REGION: ${{ vars.SCW_DEFAULT_REGION }}
      SCW_DEFAULT_ZONE: ${{ vars.SCW_DEFAULT_ZONE }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
      AWS_DEFAULT_REGION: ${{ vars.AWS_DEFAULT_REGION }}
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive
          token: ${{ secrets.ORG_REPO_PAT }}
      - uses: actions/download-artifact@v8
        with:
          name: plan-release
          path: ${{ env.TF_ROOT }}
      - working-directory: ${{ env.TF_ROOT }}
        run: |
          if [ "${{ needs.terraform-release-plan.outputs.has_changes }}" != "1" ]; then
            echo "No Terraform changes detected for release, skipping apply."
            exit 0
          fi
          terraform init -backend-config="key=release/terraform.tfstate"
          terraform apply -auto-approve "plan-release.tfplan"

  sentry-finalize-release:
    needs: [build-release-backend, build-release-frontend]
    runs-on: self-hosted
    # Release bookkeeping must not fail the deploy pipeline.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0 # set-commits --auto needs full history
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1'
      - name: Associate commits and finalize Sentry release
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ vars.SENTRY_ORG }}
        run: |
          bunx @sentry/cli@2 releases --org "$SENTRY_ORG" set-commits "${{ github.sha }}" --auto
          bunx @sentry/cli@2 releases --org "$SENTRY_ORG" finalize "${{ github.sha }}"
