{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"${AI_PIPELINE_ENABLED:-0}\" = \"1\" ]; then AI_HOST=claude-code .ai/scripts/guard-write.sh; else exit 0; fi"
          }
        ]
      }
    ]
  }
}
